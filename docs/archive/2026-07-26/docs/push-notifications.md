# Push Notifications

Push is optional.

Recommended providers:

- APNs for iOS
- FCM for Android default practicality
- UnifiedPush-compatible Android option
- ntfy-style self-hosted path where appropriate

Payload rule: push messages must say only that a new encrypted event is available. They must not include message text, sender name, conversation title, attachment name, or decrypted content.

